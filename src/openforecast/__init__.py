"""OpenForecast: the unified interface for forecasting.

The public surface is intentionally empty at this stage. Semantic types
(``TimeSeriesFrame``, ``ModelRef``, recipes, tasks) are introduced in later
stages so that no stub API outlives the design it was guessing at.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
