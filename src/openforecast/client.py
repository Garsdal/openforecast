"""The user-facing client (Step 8).

``OpenForecast`` and the module-level ``fit`` / ``forecast`` conveniences that
delegate to a default local client. This is the outermost layer: everything may
be imported from here, and nothing may import it.
"""
