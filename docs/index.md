# OpenForecast

The unified interface for forecasting.

You describe your data and the model you want in OpenForecast's own vocabulary,
and the library compiles that into whatever the underlying forecasting library
expects — statistical, neural, tree-based or a zero-shot foundation model —
behind one stable surface.

<!-- docs-exec: skip — the pitch: `train` stands for your own data, built in the quickstart -->

```python
import openforecast as of

client = of.OpenForecast()

model = client.fit("builtin/seasonal-naive", data=train, params={"season_length": 24})
forecast = client.forecast(model, data=train, horizon=24)
```

Point-in-time forecasting is first-class rather than bolted on: if you have real
historical forecast vintages — what was actually known at each origin — you can
train on them directly, and the artifact records that its origins were *observed*
rather than *simulated*.

## The invariant

> OpenForecast owns forecasting semantics. Providers only consume
> provider-neutral **execution views**. Point-in-time and ordinary event-time
> data are materialized into those views before crossing the provider boundary.

Two things follow, and they shape everything else: the core install never depends
on a forecasting framework, and no provider branches on where the data came from.
[Concepts](concepts/execution-views.md) is where that is explained; the
[architecture document](https://github.com/Garsdal/openforecast/blob/main/ARCHITECTURE.md)
states all seven rules and how each is enforced.

## How these docs are organized

| Section | Answers |
| --- | --- |
| [Getting started](getting-started/installation.md) | Install it, run the whole workflow once, learn the five ideas. |
| [Guides](guides/event-time.md) | "How do I do X?" — one task per page, with code that runs. |
| [Concepts](concepts/data-model.md) | "Why does OpenForecast work this way?" |
| [Integrations](integrations/nixtla.md) | What each provider distribution ships and how to install it. |
| [Reference](reference/generated/index.md) | Exact signatures and types, generated from the code. |

The reference section is generated by `uv run generate-reference` and diffed in
CI, so a signature cannot drift from the library. The pages beside it are written
by hand and never retype one. Every Python example in these docs is executed by
the test suite; the few that cannot be — because they need a provider
environment installed — say so in the source of the page.

## Versions

Each release publishes its own copy of this site, so `openforecast==0.1` can be
read against the documentation that shipped with it. `latest` follows the newest
release and `dev` follows `main`. The version selector is in the header, and the
[reference index](reference/generated/index.md) states the version it was
generated from.
